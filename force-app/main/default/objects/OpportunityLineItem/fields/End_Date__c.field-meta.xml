<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>End_Date__c</fullName>
    <formula>IF(
      Term_Months__c = 0,
      Start_Date__c,
  IF(
    Term_Months__c - FLOOR(Term_Months__c) = 0,
    ADDMONTHS(Start_Date__c, FLOOR(Term_Months__c)) - 1,
    Start_Date__c + (Term_Months__c * 30.44)
  )
)</formula>
    <label>End Date</label>
    <required>false</required>
    <type>Date</type>
</CustomField>
